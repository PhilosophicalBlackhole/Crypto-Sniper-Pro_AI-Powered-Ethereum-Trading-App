# Netlify configuration
# Purpose: Ensure the build uses Node 18+, runs the correct build command, and publishes from dist.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Fix dependency installation errors (rimraf@6 and others) by using Node 18 LTS
  NODE_VERSION = "18"
  # Keep alignment with local install behavior if you used legacy peers
  NPM_FLAGS = "--legacy-peer-deps"
